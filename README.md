# aquatic-labs-interview
This is a repo containing the code I did for aquatic labs full stack iot engineer position.

## Setup
To install the requirements first make a virtual environment
`python3 -m venv venv`
source the environment
`source venv/bin/activate`
then install the requirements 
`pip3 install -r requirements`

## Overview
To create a restful api for this sensor, we will be creating a 